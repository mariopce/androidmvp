<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"

    tools:context=".WordDisplayActivity$PlaceholderFragment">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Prev Button"
        android:id="@+id/prev_word_button"
        android:layout_alignParentTop="true"

         />

    <TextView android:text="@string/word"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/displayed_word_textView"
        android:layout_alignBaseline="@+id/prev_word_button"
        android:layout_alignBottom="@+id/prev_word_button"
        android:layout_toEndOf="@+id/prev_word_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Next Button"
        android:id="@+id/next_word_button"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/displayed_word_textView" />


</RelativeLayout>
